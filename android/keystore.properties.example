# Copy to android/keystore.properties (do not commit real values)
# You can also use environment variables instead.

storeFile=../keys/bloomy-release.jks
storePassword=CHANGE_ME
keyAlias=bloomy
keyPassword=CHANGE_ME
